export const zIndex = {
  HIDDEN: -100,
  // menu
  MENU_SHOWN: 80,
  MENU_HEADER_SHOWN: 90,
  // masks
  MASK_LAYOUT_SHOWN: 180,
  MASK_PAGE_SHOWN: 190,
  MASK_VIEWPORT_SHOWN: 200,
};

export const duration = {
  FADE_IN: 0.5,
  DELAY_ANIMATE_IN: 1.75,
};

export const locoOptions = {
  lerp: 0.05,
  smooth: true,
  multiplier: 1.25,
};
