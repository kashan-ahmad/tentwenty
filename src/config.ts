export const zIndex = {
  HIDDEN: -100,
  // layouts
  MASK_LAYOUT_SHOWN: 100,
  // menu
  MENU_SHOWN: 90,
  // masks
  MASK_PAGE_SHOWN: 200,
};

export const duration = {
  DELAY_ANIMATE_IN: 1.75,
};
