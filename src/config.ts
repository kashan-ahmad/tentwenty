export const zIndex = {
  HIDDEN: -100,
  // layouts
  MASK_LAYOUT_SHOWN: 100,
  // menu
  MENU_SHOWN: 80,
  MENU_HEADER_SHOWN: 90,
  // masks
  MASK_PAGE_SHOWN: 200,
};

export const duration = {
  FADE_IN: 0.5,
  DELAY_ANIMATE_IN: 1.75,
};
